<UserControl x:Class="BuildNotifications.Views.Sight.SightSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:BuildNotifications.ViewModel.Sight"
             xmlns:local="clr-namespace:BuildNotifications.Views.Sight"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance d:Type={x:Type viewModel:SightSelectionViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <ItemsControl ItemsSource="{Binding Sights}">
        <ItemsControl.Resources>
            <DataTemplate DataType="{x:Type viewModel:BaseSightViewModel}">
                <local:BaseSightView />
            </DataTemplate>
        </ItemsControl.Resources>
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel IsItemsHost="True" Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
</UserControl>