<UserControl x:Class="BuildNotifications.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:animation="clr-namespace:BuildNotifications.Resources.Animation"
             xmlns:text="clr-namespace:BuildNotifications.Resources.Text"
             mc:Ignorable="d">

    <Grid Width="{StaticResource BlockDouble}">
        <text:DecoratedTextBox Text="{Binding Value, Mode=TwoWay}" Icon="Search"
                               Margin="{DynamicResource BlockQuarterXOnlyMargin}"/>
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="GotKeyboardFocus">
                <animation:WidthChange TargetWidth="{StaticResource BlockFourteenTimes}" />
            </i:EventTrigger>
            <i:EventTrigger EventName="LostKeyboardFocus">
                <animation:WidthChange TargetWidth="{StaticResource BlockDouble}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </Grid>
</UserControl>